# --- Server ---
PORT=4000
CLIENT_URL=http://localhost:5173

# --- Database ---
# Live cloud data (MongoDB Atlas) — same database the live site uses.
# Local `npm run dev` connects to this and sees the SAME products/orders/settings.
MONGODB_URI=mongodb+srv://velourauser:Hushae2026db@cluster0.7lcpatb.mongodb.net/hushae

# --- Auth ---
# JWT signing secret (production value, kept in sync)
JWT_SECRET=h7Xk9mP2vQ8sL4nR6tW3yZ5bC1dF0eGj
JWT_EXPIRES=7d

# --- Admin account ---
# Used the FIRST time the DB is empty AND SEED_ON_START=true.
# For the live cloud DB the admin already exists with these credentials.
ADMIN_EMAIL=admin@hushae.pk
ADMIN_PASSWORD=WDD7xvY8v2qTwXsv

# --- Seed control ---
# Set to "true" ONLY the first time you set up a fresh empty database.
# Never leave this "true" in normal use, otherwise a fresh empty DB will be
# repopulated with demo data on the next startup.
SEED_ON_START=false
